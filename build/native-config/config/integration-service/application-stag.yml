spring:
  config:
    activate:
      on-profile: stag

server:
  port: 8082

# Cấu hình các URL của service khác trong nội bộ môi trường Staging
app:
  services:
    ecommerce: http://f2c-ecommerce-stag:8080
    iam: http://f2c-iam-stag:8081

logging:
  level:
    com.f2c.integration: INFO